<!--
TEMPLATE: Patch Status
INSTALLS TO: .docs/patches/<patch-id>/status.md
PROFILE: minimal
GENERATED BY: patch:status  (regenerate; do not hand-edit the machine block)
PURPOSE: A <60-second scan of where the patch stands. Local proof and CI/external review
         are deliberately separate. Stdout of patch:status is the JSON block; this Markdown
         is the human render of the same truth.
-->
# Status · {{patch_spec_id}}

```json
{
  "patch_spec_id": "{{patch_spec_id}}",
  "profile": "{{minimal | review-proof}}",
  "status": "{{draft | active | local-accepted | blocked | handoff | closed}}",
  "generated_at": "{{ISO8601}}",
  "local_acceptance": "{{not_run | pending | passed | failed}}",
  "ci": "{{not_run | pending | green | red}}",
  "external_review": "{{not_run | pending | clear | changes_requested}}",
  "proof_gaps": {{count}}
}
```

| | |
|:--|:--|
| **Status** | `{{status}}` |
| **Profile** | `{{profile}}` |
| **Patch** | {{short_title}} |
| **Branches** | `{{donor_head}}` → `{{target_head}}` |
| **Repo law** | `{{AGENTS.md \| CLAUDE.md}}` |
| **Updated** | {{ISO8601}} |

## Capabilities enabled

<!-- Driven by .docs/patch-kit/config.json. Show only what the profile actually has. -->
- [x] `patch:truth:check`
- [x] `patch:classify`
- [x] `patch:accept`
- [{{x| }}] `patch:review:coderabbit`  <!-- review-proof -->
- [{{x| }}] `patch:proof:playwright` / `patch:proof:runtime`  <!-- review-proof -->
- [{{x| }}] `patch:drift:analyze` / `patch:diff:snapshot`  <!-- review-proof -->

## Artifact tree

| Artifact | Present | Path |
|:--|:--:|:--|
| Charter | `{{✓\|—}}` | `patch.md` |
| Classification | `{{✓\|—}}` | `.docs/patches/{{patch_spec_id}}/classification.json` |
| Acceptance run | `{{✓\|—}}` | `.docs/patches/{{patch_spec_id}}/runs/accept-latest.json` |
| Review triage | `{{✓\|—\|n/a}}` | `.docs/patches/{{patch_spec_id}}/review-triage.md` |
| Proof bundle | `{{✓\|—\|n/a}}` | `.docs/patches/{{patch_spec_id}}/proof-bundle.md` |
| Handoff | `{{✓\|—}}` | `.docs/patches/{{patch_spec_id}}/handoff-pr-summary.md` |

## Proof ledger

<!-- The heart of status: every claim, its state, and whether it is LOCAL or EXTERNAL. -->
| Signal | Scope | State | Note |
|:--|:--|:--|:--|
| Local acceptance | local | `{{passed \| pending \| failed}}` | {{last run ref}} |
| Regression test | local | `{{passed \| pending \| failed}}` | {{…}} |
| CodeRabbit review | external | `{{clear \| changes_requested \| pending \| n/a}}` | external CLI |
| CI pipeline | external | `{{green \| red \| pending \| not_run}}` | {{provider}} |

## Open proof gaps

<!-- If zero, write "None." Each gap is a reason the patch is not yet handoff-ready. -->
1. {{gap — e.g. "CI not yet run on target branch"}}
2. {{…}}

> **Reading rule.** `local-accepted` ≠ shippable. The patch is handoff-ready only when proof
> gaps are zero **or** each remaining gap is explicitly accepted in the handoff summary.
