<!--
TEMPLATE: Acceptance Run
INSTALLS TO: .docs/patches/<patch-id>/runs/accept-latest.json
PROFILE: minimal
GENERATED BY: patch:accept  (latest local acceptance record; overwritten on rerun)
PURPOSE: A machine-readable record of the latest acceptance attempt — commands, exit codes, output
         summaries, the LOCAL result, and known gaps. Local acceptance is not CI.
-->
# Acceptance run · {{patch_spec_id}} · {{ISO8601}}

```json
{
  "patch_spec_id": "{{patch_spec_id}}",
  "run_at": "{{ISO8601}}",
  "result": "{{passed | failed}}",
  "scope": "local",
  "ci_implied": false,
  "commands": {{n}},
  "failed_commands": {{n}},
  "known_gaps": {{n}}
}
```

> **Scope: local.** A `passed` result here means the acceptance commands succeeded on this
> machine/worktree. It does **not** imply CI is green or external review is clear.

## Environment

| | |
|:--|:--|
| Worktree | `{{worktree}}` |
| Branch | `{{target_head}}` @ `{{short_sha}}` |
| Runner | `{{uv version / node version / etc.}}` |
| Dry-run first | `{{yes \| no}}` |

## Commands

| # | Command | Exit | Result |
|:--:|:--|:--:|:--|
| 1 | `{{package_runner}} run patch:truth:check` | `{{0}}` | `{{ok}}` |
| 2 | `{{project_test_command}}` | `{{0}}` | `{{ok}}` |
| 3 | `{{package_runner}} run patch:accept --dry-run` | `{{0}}` | `{{plan ok}}` |
| 4 | `{{package_runner}} run patch:accept` | `{{0}}` | `{{accepted}}` |

### Output summaries

<!-- Keep to signal. Trim long logs; link the full log if retained. -->
**`{{project_test_command}}`**
```text
{{e.g. Test Files  3 passed (3) · Tests  41 passed (41) · Duration 6.2s}}
```

**`{{package_runner}} run patch:accept`** (stdout — machine-readable)
```json
{ "accepted": true, "patch_spec_id": "{{patch_spec_id}}", "checks": {{n}}, "scope": "local" }
```

<!-- If any command failed, show its stderr here. Structured errors exit non-zero. -->

## Known gaps (not blocking local acceptance)

1. {{gap, e.g. "CI not yet triggered on target"}} → tracked in handoff
2. {{…}}

## Follow-up actions

- [ ] {{action}}
- [ ] {{push branch and let CI run}}
