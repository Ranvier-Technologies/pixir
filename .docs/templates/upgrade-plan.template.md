<!--
TEMPLATE: Upgrade Plan
INSTALLS TO: .docs/patch-kit/upgrade-plan.md
PROFILE: minimal
GENERATED BY: plan_patch_operator_upgrade.py  (conservative; never mutates without approval)
PURPOSE: Show what a kit upgrade would change, with human-review cases made loud.
STATUS VOCABULARY:
  unchanged        identical, skip
  upgrade          safe kit-owned update, auto-applicable
  modified         repo edited a kit-owned file — HUMAN REVIEW
  missing          kit file absent in repo — will be added
  new              new in this kit version — will be added
  repo_owned       repo claims ownership — HUMAN REVIEW, never auto-overwritten
  refresh-manifest manifest metadata only
-->
# Kit upgrade plan · {{current_version}} → {{target_version}}

```json
{
  "from": "{{current_version}}",
  "to": "{{target_version}}",
  "generated_at": "{{ISO8601}}",
  "requires_human_review": {{n}},
  "auto_applicable": {{n}},
  "counts": { "unchanged": {{n}}, "upgrade": {{n}}, "modified": {{n}}, "missing": {{n}}, "new": {{n}}, "repo_owned": {{n}}, "refresh-manifest": {{n}} }
}
```

> Run with `--dry-run` (default). Nothing is written until an operator approves the
> `upgrade` set and resolves every `modified` / `repo_owned` case below.

## ⚠ Requires human review

<!-- Surfaced first, on purpose. These are NEVER auto-applied. -->
| File | Status | Why | Suggested action |
|:--|:--|:--|:--|
| `{{path}}` | `modified` | repo edited a kit-owned file | {{diff & re-apply, or claim repo_owned}} |
| `{{path}}` | `repo_owned` | repo owns this file | {{confirm keep; kit will not touch}} |

## Auto-applicable

| File | Status |
|:--|:--|
| `{{path}}` | `upgrade` |
| `{{path}}` | `new` |
| `{{path}}` | `missing` → add |

## No action

| Status | Count | Notes |
|:--|:--:|:--|
| `unchanged` | {{n}} | identical to kit |
| `refresh-manifest` | {{n}} | manifest metadata only |

## Apply

```bash
# review only (default)
uvx patch-operator-bootstrapper plan-upgrade --to {{target_version}} --dry-run
# apply the auto set after resolving review cases
uvx patch-operator-bootstrapper apply-upgrade --to {{target_version}} --only upgrade,new,missing
```
